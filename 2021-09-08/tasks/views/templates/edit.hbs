<!doctype html>
<html lang="en">
  {{>head}}
  <body>
    {{>nav}}
    <main class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Radaguoti UŽDUOTĮ</h5>
                    <form method="POST" action="/edit?id={{task._id}}">
                        <div class="mb-3">
                            <label class="form-label">Užduotis</label>
                            <input type="text" name="task" class="form-control" value="{{task.task}}">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Statusas</label>
                            <select class="form-control" name="status">
                                <option value="{{task.status}}">"{{task.status}}"</option>
                                {{#each statuses}}
                                    <option value="{{name}}">{{name}}</option>
                                {{~/each}}
                                
                            </select>
                        </div>
                        
                        <div class="mb-3">
                            <label class="form-label">Prioritetas</label>
                            <select class="form-control" name="priority" >
                                <option value="{{task.priority}}">"{{task.priority}}"</option>
                                {{#each priorities}}
                                    <option value="{{name}}">{{name}}</option>
                                {{~/each}}
                                
                            </select>
                        </div>
                        <div class="mb-3">
                            <button class="btn btn-success" type="submit">Atnaujinti</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    </main>
     {{>foot}}   
  </body>
</html>